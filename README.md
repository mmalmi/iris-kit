# Iris Kit

Shared Iris packages for standalone apps.

The first package is `@iris/svelte-ui`, a small Svelte component package for reusable identity UI. App-specific routing, profile fetching, badges, media behavior, and release scripts stay in the app repos unless they become broadly reusable.

